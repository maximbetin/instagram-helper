# GUI-specific requirements for Instagram Helper
# These are in addition to the main project requirements

# Tkinter is usually included with Python, but some systems may need it
# For Ubuntu/Debian: sudo apt-get install python3-tk
# For CentOS/RHEL: sudo yum install tkinter
# For macOS: Usually included with Python

# Main project dependencies (these should already be installed)
playwright>=1.40.0
python-dotenv>=1.0.0
freezegun>=1.2.0
pytest>=7.0.0
pytest-cov>=4.0.0
